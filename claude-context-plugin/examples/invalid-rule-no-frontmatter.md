# Invalid Rule Example - No Frontmatter

This file has no frontmatter at all.

If you try to add this with `/claude-context-updater:ctx-rule add`, it should fail with:
"Source file must have frontmatter"
