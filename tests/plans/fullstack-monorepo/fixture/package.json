{
  "name": "fullstack-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "A fullstack monorepo with Next.js app and database library for testing multi-type projects",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
