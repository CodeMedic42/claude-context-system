# Use pnpm for package management
# This file configures pnpm behavior for the monorepo

# Use hoisted node_modules for better compatibility with tools like lerna
node-linker=hoisted

# Strict peer dependencies
strict-peer-dependencies=false

# Auto install peers
auto-install-peers=true

# Shamefully hoist to fix lerna dependency issues
shamefully-hoist=true
